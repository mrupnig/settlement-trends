[project]
name = "settlement-trends"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
edition = "edition.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"